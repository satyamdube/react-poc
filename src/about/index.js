import React from 'react';
import {share} from '../Icon';
import Header from '../header';

const About = () => {
  return (
    <React.Fragment>
      <Header/>
      <div>About {share}</div>
   </React.Fragment>
  );
}

export default About; 